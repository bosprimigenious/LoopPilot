# Maintenance script scaffold

Only installation, migration, backup, fixture, scheduler, and regression utilities belong here. Core Loop behavior belongs in `src/loop_pilot/` and must be exposed through the `loop-pilot` CLI.

Required and optional scripts are specified in `docs/development/14-implementation-manifest.md`.
