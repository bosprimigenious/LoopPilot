# Configuration scaffold

Cursor should add validated example configurations here only when implementing Mini. Follow `docs/development/24-configuration-spec.md`.

Real credentials belong in ignored environment files or the operating-system credential store, never in YAML.
