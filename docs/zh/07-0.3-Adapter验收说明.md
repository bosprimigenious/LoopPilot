# 07 0.3 Adapter 验收说明

本文用中文说明 **0.3 Adapter MVP** 的验收分层、当前状态与未完成项。英文权威清单见 [development/36-adapter-mvp-0.3-acceptance.md](../development/36-adapter-mvp-0.3-acceptance.md)。

## 版本与分支

| 项 | 值 |
|----|-----|
| 版本标签 | `v0.3.0a1`（发布线）+ **`v0.3.0b1`**（ToolBroker 闭环） |
| 分支 | `adapter-mvp-0.3-b1` |
| 前置 | 0.2 已验收 `v0.2.0a1` |
| 英文验收清单 | [36-adapter-mvp-0.3-acceptance.md](../development/36-adapter-mvp-0.3-acceptance.md) |
| b1 交付记录 | [logs/2026-06-21-0.3-b1-delivery.md](../development/logs/2026-06-21-0.3-b1-delivery.md) |
| 手动 live 模板 | [logs/2026-06-21-0.3-live-adapter-manual-check.md](../development/logs/2026-06-21-0.3-live-adapter-manual-check.md) |

## 发布线（0.3.0a1 → 0.3.0b1）

| 项 | 说明 |
|----|------|
| 0.3.0a1 完成点 | 提交 `0988f99`，tag `v0.3.0a1` |
| 0.3.0b1 完成点 | ToolBroker 全 Loop + audit + bypass 测试 |
| b1 规格 | [44-0.3b1-toolbroker-live-spec.md](../development/44-0.3b1-toolbroker-live-spec.md) |

## 0.3.0b1 交付项（2026-06-21）

| # | 项 | 状态 |
|---|-----|------|
| 1 | Intern `_run_pytest` / `_apply_fix` 经 ToolBroker | 完成 |
| 2 | Paper working-copy 读写经 ToolBroker | 完成 |
| 3 | DailyNews connector HTTP 经 ToolBroker | 完成 |
| 4 | 集成测试：Loop 路径绕过 broker 须失败 | 完成 |
| 5 | `tool-results.json` 与 broker audit 对齐 | 完成 |
| 6 | 各 Loop 受控 manual live run | 模板已更新 — **待本机填写** |
| 7 | 39-next-steps Phase 4 ToolBroker | 完成 |

## 0.3 证明什么

0.3 **不等于**旧称「V1」。本阶段只证明：

- Mock 之外的真实 Adapter（Cursor CLI、OpenAI-compatible）能在**严格门禁**下接入
- ModelRouter、ToolBroker、默认 `allow_real_adapters=false` 生效
- 0.1 / 0.2 回归不退化
- **0.3.0b1**：Loop 侧效应经 ToolBroker 审计

**不属于 0.3**：SQLite 恢复、inbox/queue/today、审批 CLI、调度、PyPI、团队功能（见 [08-0.4-个人日用闭环.md](08-0.4-个人日用闭环.md)）。

## 三层验收（L1 / L2 / L3）

### L1 — 0.1 基线回归（自动化）

| 内容 | 期望 |
|------|------|
| `pytest -q` | 全绿（当前 **130 passed**） |
| fixture dry-run | intern / paper / daily-news 各可跑 |
| `run all --fixture-set mini --dry-run` | 成功 |

**状态（2026-06-21）**：通过。

### L2 — 0.2 demo 回归（自动化）

**状态（2026-06-21）**：通过。

### L3 — 0.3 Adapter 层（自动化 + 手动）

**自动化已通过：**

| 层 | 范围 | 结果 |
|----|------|------|
| Adapter CLI | `adapters list`、`adapters doctor` | 通过 |
| 安全门禁 | 默认 blocked；unknown adapter 显式 BLOCKED | 通过 |
| ToolBroker | 全 Loop 路由 + bypass 测试 + audit | 通过 |
| CI verify | `scripts/verify_0_3_acceptance.py` | 20/20 PASS |

**手动 / Full DoD 仍缺：**

| 项 | 说明 |
|----|------|
| Intern 受控真实 run | `--allow-write --allow-real-adapters` + Cursor CLI |
| Paper 受控真实 run | 需 `DEEPSEEK_API_KEY` |
| DailyNews live-small | 可选 live 源 |

## 当前结论（2026-06-21）

| 口径 | 结论 |
|------|------|
| **0.3.0a1 发布线** | 通过 |
| **0.3.0b1 ToolBroker** | **已交付** |
| **Full 0.3 DoD** | **部分完成** — MANUAL live run 待本机 |

一句话：**ToolBroker 全 Loop 强制与 audit 已闭环；默认 gate 与回归全绿；受控真实 Adapter run 留 MANUAL 填写。**

## 常用命令

```bash
pytest -q
python scripts/verify_0_3_acceptance.py
loop-pilot adapters list
loop-pilot adapters doctor
loop-pilot run all --fixture-set mini --dry-run
loop-pilot run all --profile demo --dry-run

# 真实 run（MANUAL；需 env + --allow-real-adapters）
loop-pilot run intern --workspace examples/intern_demo --allow-write --allow-real-adapters
loop-pilot run paper --workspace examples/paper_demo --adapter deepseek --allow-real-adapters --allow-write
```

## 下一步

1. 本机完成 [live 模板](../development/logs/2026-06-21-0.3-live-adapter-manual-check.md) MANUAL 项
2. 进入 **0.4** — 见 [08-0.4-个人日用闭环.md](08-0.4-个人日用闭环.md)
