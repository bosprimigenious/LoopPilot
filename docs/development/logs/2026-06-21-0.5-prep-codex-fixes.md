# 2026-06-21 — 0.5-prep Codex 修复日志

> Branch: `feat/0.5-safe-autonomy`  
> Context: PR #7 Codex review — fail-closed prep scaffolding

## Codex 发现（修复前）

| 优先级 | 问题 | 修复前状态 |
|--------|------|------------|
| P0 | `schedule install --yes` 在 prep 阶段调用 `schtasks /Create` | ❌ 无 readiness gate，allow_install 时真写 OS |
| P0 | `schedule uninstall --yes` 无 gate 即调用 `schtasks /Delete` | ❌ 直接 subprocess |
| P0 | `run daily --unattended --safe` 在 prep 可执行 | ❌ CLI 无此路径 / 无 BLOCKED |
| P0 | Readiness gate fail-closed | ❌ 无 `readiness.py` |
| P0 | SafetyGate prep 阶段 deny Level 3+ | ❌ 仅 policy 级检查 |
| P1 | cron/systemd marker-only 误报 installed | ❌ `installed.json` + `installed: true` |
| P1 | InstallStatus PREVIEWED/BLOCKED/INSTALLED | ❌ 无枚举 |
| P2 | sync 时 deferred review 被重置为 pending | ❌ `upsert_pending` 覆盖 deferred |

## 已修复

1. **`src/loop_pilot/safety/readiness.py`** — `safety.stage` 默认 `prep`；`PREP_STAGE_BLOCKED` 常量
2. **`SafetyGate`** — prep 阶段阻断 `schedule.install` / `schedule.uninstall` / `unattended.daily`；Level 3+ adapter 调用阻断
3. **`installer.py`** — prep 阶段拒绝 OS 变更；cron/systemd 写 `install_status=previewed`；Windows 仅在 `stage=ready` 时 schtasks
4. **`cli_schedule.py`** — install/uninstall 经 SafetyGate；输出 `BLOCKED:` 前缀
5. **`cli.py`** — `--unattended` / `--safe` / `--level`；prep 下明确 BLOCKED stub
6. **`review/store.py`** — `upsert_pending` 保留 `deferred` 状态与 `deferred_until`
7. **`scripts/verify_0_5_prep.py`** — 报告 `0.5-prep: PASS` / `0.5-ready: NOT READY`
8. **测试** — `test_0_5_prep_safety.py` + 集成/单元测试更新

## 验收

```bash
ruff check .
python -m pytest -q                                    # 219 passed
python scripts/verify_0_4b_acceptance.py               # 27/27
python scripts/verify_0_4c_acceptance.py               # 22/22
python scripts/verify_0_5_prep.py                      # 0.5-prep: PASS
```

## 措辞

- 当前交付为 **0.5-prep only**（安全脚手架 + fail-closed 阻断）
- **不是**「0.5 已实现」— 真实 unattended pipeline 与 ready-stage OS install 待 0.5-b 与 readiness gate

## 下一步（0.5-ready 之后）

1. Truthful 0.4 Milestone A 全绿
2. `safety.stage=ready` + readiness 脚本通过
3. 0.5-b：`run daily --unattended --safe` 真实 pipeline
4. 0.5-a ready-stage：Windows schtasks 真装 + 5 天 dogfood
