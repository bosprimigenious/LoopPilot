# 0.3 Adapter MVP Acceptance Run Log

**Date:** 2026-06-21  
**Branch:** `main` (expected `adapter-mvp-0.3` or `main`)  
**Package version:** `0.3.0a1`  
**Runner:** automated acceptance (Cursor agent)

## Environment

| Item | Result |
|------|--------|
| `git branch --show-current` | `main` |
| `python -m pip install -e ".[dev]"` | PASS |
| `ruff check .` | PASS (All checks passed) |
| `pytest -q` | PASS (109 passed) |
| `loop-pilot doctor` | PASS |

## Layer results

| Layer | Scope | Result | Evidence |
|-------|-------|--------|----------|
| **基础** | install / lint / test / doctor | **PASS** | 109 tests green; doctor OK |
| **第一层** | 0.1 fixture regression | **PASS** | intern succeeded; paper partial; daily_news succeeded; `run all --fixture-set mini` OK; candidate files present under latest daily-news run |
| **第二层** | 0.2 demo profile regression | **PASS** | intern succeeded; paper partial; daily_news succeeded; `run all --profile demo` OK |
| **第三层** | 0.3 static files | **PASS** | `src/loop_pilot/adapters/` (mock, cursor_cli, openai_compatible, factory, preflight, doctor); `src/loop_pilot/tools/broker.py`; `src/loop_pilot/connectors/local_json.py`; `config/adapters.yaml` |
| **第四层** | Adapter CLI | **PASS** | `adapters list` shows mock + real entries disabled; `adapters doctor` exit 1 with blocked real adapters (expected default) |
| **第五层** | Safety gate (default) | **PASS** | `run intern ... --adapter cursor_cli --dry-run` → `blocked`, reason contains `allow_real_adapters=false` |
| **第六层** | Missing API key | **PASS*** | Default: blocked by `allow_real_adapters=false`. With `--allow-real-adapters`: `blocked`, reason `missing API key (DEEPSEEK_API_KEY not set)`, no Traceback |
| **第七层** | ToolBroker / gating tests | **PASS** | `test_adapter_gating` 3/3; `test_tool_broker_policy` 4/4; `test_secret_redaction` 3/3; `test_adapter_blocked_by_default` 2/2 |
| **第八层** | Cursor CLI doctor | **MANUAL** | `cursor-agent` not in PATH on this host |
| **第九层** | config.local real dry-run | **MANUAL** | Not run (no config.local committed) |
| **第十层** | DeepSeek live / DailyNews live-small / secret grep | **SKIP/MANUAL** | No DEEPSEEK_API_KEY; live-small not run; trace grep found no raw secrets in sampled artifacts |

\*Layer 6 per spec without `--allow-real-adapters` hits the outer safety gate first; missing-key behavior verified with explicit opt-in flag.

## Command transcript (selected)

```text
# Layer 4
loop-pilot adapters list
  mock                     mock               enabled (mock)
  cursor_cli               cursor_cli         disabled (allow_real_adapters=false)
  deepseek                 openai_compatible  disabled (allow_real_adapters=false)
  ...

loop-pilot adapters doctor  → exit 1 (real adapters blocked — expected)

# Layer 5
loop-pilot run intern --workspace examples/intern_demo --adapter cursor_cli --dry-run
  → completed: blocked
  → Reason: ... allow_real_adapters=false

# Layer 6 (opt-in)
loop-pilot run paper --workspace examples/paper_demo --adapter deepseek --allow-real-adapters --dry-run
  → completed: blocked
  → Reason: ... missing API key (DEEPSEEK_API_KEY not set)

# Layer 1 candidate artifacts (daily_news mini run)
var/artifacts/daily-news/20260621T022201.282593Z-daily_news-5ce52284/
  intern-candidates.md, paper-candidates.md, candidate-actions.json（OK）
```

## Notes

1. One parallel Layer-1 intern fixture run hit `PermissionError` on `var/state/locks/loop_intern.lock`; isolated re-run succeeded (lock contention, not functional regression).
2. `grep allow_real_adapters` → `config/loop-pilot.yaml: allow_real_adapters: false`（OK）
3. Implementation gaps vs full 0.3 DoD: no documented real Intern/Paper/DailyNews controlled live runs; ToolBroker not yet wired as sole path inside all loops.

## Verdict

**Safety MVP (0.3.0a1 alpha): PASS** — default gate, CLI surface, and regression hold.  
**Full 0.3 DoD: NOT YET** — real adapter controlled runs and complete ToolBroker loop integration remain open.
