# 0.3 可执行验收运行日志

**日期:** 2026-06-21  
**分支:** `adapter-mvp-0.3`  
**包版本:** `0.3.0a1`  
**环境:** Windows 10, Python 3.12, `C:\AIModel_Zoo\LoopPolit`  
**DEEPSEEK_API_KEY:** 未设置  
**Cursor CLI:** 未验证 live（MANUAL）

## 核心判定

> 真实 Adapter 是否被安全接入，同时系统仍然稳定、可控、可回退。

**结论:** 默认门控下 **安全接入合格**（真实 adapter 被 auditable BLOCKED）；0.1/0.2 回归全绿；完整 0.3 DoD（各 loop 一次 documented controlled real run）**未达**。

---

## 环境准备

| 项 | 结果 |
|----|------|
| `git branch --show-current` | `adapter-mvp-0.3` |
| `python -m pip install -e ".[dev]"` | PASS — `loop-pilot==0.3.0a1` |
| `config/loop-pilot.yaml` `allow_real_adapters` | `false`（默认） |

---

## L1：0.1 + 0.2 回归 — **PASS**

### 1.1 Mini fixture

| 命令 | 结果 | 摘要 |
|------|------|------|
| `loop-pilot run intern --fixture simple_python_bug --dry-run` | PASS | `succeeded` — All tests passed after controlled fix |
| `loop-pilot run paper --fixture unsupported_claim --dry-run` | PASS | `partial` — Claim requires additional source（非 succeeded，符合 SOURCE_REQUIRED） |
| `loop-pilot run daily-news --fixture github_star_snapshots --dry-run` | PASS | `succeeded` — Processed 4 items from day2 |
| `loop-pilot run all --fixture-set mini --dry-run` | PASS | daily_news/intern `succeeded`, paper `partial` |

**daily_news 三候选文件**（`20260621T023139.660299Z-daily_news-d95c4534`）:

- [x] `intern-candidates.md`
- [x] `paper-candidates.md`
- [x] `candidate-actions.json`

### 1.2 0.2 workspace / demo profile

| 命令 | 结果 | 摘要 |
|------|------|------|
| `loop-pilot run intern --workspace examples/intern_demo --dry-run` | PASS | `succeeded` |
| `loop-pilot run paper --workspace examples/paper_demo --dry-run` | PASS | `partial` |
| `loop-pilot run daily-news --source-profile demo --dry-run` | PASS | `succeeded` — 2 items profile demo |
| `loop-pilot run all --profile demo --dry-run` | PASS | daily_news/intern succeeded, paper partial |

**判定:** 无 crash；Paper 未 succeeded；daily_news artifacts 完整。

---

## L2：Adapter 核心 — **PASS**（live real 为 MANUAL）

| 命令 | 结果 | 摘要 |
|------|------|------|
| `loop-pilot adapters list` | PASS | mock enabled; cursor_cli/deepseek disabled `(allow_real_adapters=false)` |
| `loop-pilot adapters doctor` | PASS | exit 0 — mock OK; real adapters `blocked` by default |
| `loop-pilot run intern --workspace examples/intern_demo --adapter cursor_cli --dry-run` | PASS | `blocked` — `adapter cursor_cli blocked by allow_real_adapters=false` |

**Artifact 检查**（run `20260621T023429.452626Z-intern-2aed589e`）:

| 文件 | 状态 | 说明 |
|------|------|------|
| `adapter-call-trace.jsonl` | 缺失 | **预期** — 在 Registry 门控处 BLOCKED，未调用 adapter |
| `review-required.md` | 缺失 | intern blocked 早退，非 daily_news 路径 |
| `next-actions.md` | 缺失 | 同上 |
| `trace.jsonl` | 存在 | OBSERVING → FINALIZING → terminated blocked |

**DeepSeek:**

| 命令 | 结果 | 摘要 |
|------|------|------|
| `loop-pilot run paper --workspace examples/paper_demo --adapter deepseek --dry-run` | PASS (gate) | `blocked` — 外层 `allow_real_adapters=false`（先于 missing key） |
| `--allow-real-adapters` + 无 key | **SKIP MANUAL** | 历史 state 有 `missing API key (DEEPSEEK_API_KEY not set)` 无 Traceback；本次未跑 opt-in 路径 |

---

## L3：安全 — **PASS**（1 项 WARN）

| 命令 | 期望 | 结果 |
|------|------|------|
| `run intern ... --adapter cursor_cli --dry-run`（默认 gate） | blocked | PASS — `allow_real_adapters=false` |
| `run paper ... --adapter deepseek --dry-run`（无 key） | blocked, 无 traceback | PASS — 外层 gate blocked（非 missing-key 路径） |
| `run intern --adapter fake_adapter --dry-run` | 不 crash | PASS（WARN）— `succeeded` via mock 回退，非显式 BLOCKED |
| `pytest -q` | 全绿 | PASS — **109 passed** |
| `ruff check .` | 全绿 | PASS |
| `loop-pilot doctor` | OK | PASS — MockAdapter default |
| grep artifacts/state 无 API key 泄露 | N/A / 无 sk- | PASS — 仅 env 名出现在 blocked reason，无 `sk-` 模式 |

---

## §五 最终总命令块（2026-06-21 实际执行）

```bash
python -m pip install -e ".[dev]"
loop-pilot run intern --fixture simple_python_bug --dry-run
loop-pilot run paper --fixture unsupported_claim --dry-run
loop-pilot run daily-news --fixture github_star_snapshots --dry-run
loop-pilot run all --fixture-set mini --dry-run
loop-pilot run intern --workspace examples/intern_demo --dry-run
loop-pilot run paper --workspace examples/paper_demo --dry-run
loop-pilot run daily-news --source-profile demo --dry-run
loop-pilot run all --profile demo --dry-run
loop-pilot adapters list
loop-pilot adapters doctor
loop-pilot run intern --workspace examples/intern_demo --adapter cursor_cli --dry-run
loop-pilot run paper --workspace examples/paper_demo --adapter deepseek --dry-run
pytest -q
ruff check .
loop-pilot doctor
python scripts/verify_0_3_acceptance.py
```

**汇总:** 上述命令均无 crash；回归 outcome 符合预期；adapter 默认 BLOCKED；自动化脚本 **20/20 PASS**。

---

## §六「✅ 必须通过」对照

| 项 | 状态 |
|----|------|
| L1 0.1+0.2 回归全绿 | ✅ PASS |
| L2 adapters list/doctor + cursor_cli 门控 blocked | ✅ PASS |
| L2 DeepSeek live artifact | ⏭ MANUAL（无 key） |
| L3 默认 gate 阻断 real adapter | ✅ PASS |
| L3 无 traceback / 无 key 泄露 | ✅ PASS |
| L3 pytest + ruff + doctor | ✅ PASS |
| 各 loop ≥1 controlled **real** run（opt-in） | ❌ 未执行 |
| ToolBroker 全 loop 强制 | ❌ 未闭环（单元测试 PASS） |

---

## 已知差距 / 最小修复项

1. **unknown `--adapter`**（如 `fake_adapter`）静默回退 mock → 应显式 BLOCKED/FAILED 并记录原因。
2. **DeepSeek missing-key** 需在 `--allow-real-adapters` 下验收；无 credentials 标 MANUAL，不宣称 live 通过。
3. **完整 DoD:** Intern/Paper/DailyNews 各一次 documented controlled real run；ToolBroker loop 集成。

---

## 自动化

```bash
python scripts/verify_0_3_acceptance.py
```

不含 DeepSeek live、不含 `--allow-real-adapters`；CI 可跑。

---

## 总判定

| 层级 | 结果 |
|------|------|
| L1 | **PASS** |
| L2 | **PASS**（DeepSeek live: MANUAL） |
| L3 | **PASS**（fake_adapter: WARN） |

**一句话:** **0.3 安全 alpha 合格，完整 DoD 未达** — 真实 adapter 默认被安全门控阻断，回归稳定可控；controlled real run 与 ToolBroker loop 闭环仍待补。
